List of shows:
<div>
  <% @shows.each do |show| %>
    <div class="small-3 columns">
      <%= link_to image_tag(show.poster, alt: "Poster", class: "poster"), show_path(show) %>
      <div><%= show.name %></div>
    </div>
  <% end %>
</div>

  <%= paginate @shows %>

