Here is a list of users:
<ul>

 <% @users.each do |user| %>
  <li><%= link_to user.username, admin_user_path(user) %>
  <%= link_to "Delete user", admin_user_path(user), method: :delete %></li>

</ul>
<% end %>


